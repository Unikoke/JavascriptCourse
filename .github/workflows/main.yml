name: Build and Deploy
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build --if-present

      - name: Deployment Action
        uses: chrnorm/deployment-action@55729fcebec3d284f60f5bcabbd8376437d696b1
        # If the action requires inputs, add them under `with:` here.
        # Do NOT commit secrets. Configure any required secrets via the repository's Settings -> Secrets.
